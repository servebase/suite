啟動時 ( npm start / npm run dev ) 使用 start script
start script 有個 loop
這是基板服務  很多 derived project 都用同樣方式, 有些問題

1. 不易辨識: ps 下來都一樣
2. 殺不死: 有 loop restart, 要殺外面的 start script

我在想
 1. 至少帶個 pwd 或 dirname 
 2. 怎樣讓 script + inner cmd 容易被當成一體
 3. 考慮弄成 service 形式? 這還能接到 stdout 嗎
    也許也可以有個 > log > cat 即時看結果的窗方便我們 debug?

